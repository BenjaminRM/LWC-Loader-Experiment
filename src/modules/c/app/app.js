import { LightningElement, api, track } from 'lwc';

export default class App extends LightningElement {

    // @track showSimple = true;
    // @track showFancy = false;
    // timeShown;

    connectedCallback() {
        // let spinnerChoice = Math.floor(Math.random() * 2);
        // if(spinnerChoice == 0) { this.showSimple = true; }
        // if(spinnerChoice == 1) { this.showFancy = true; }

        // this.timeShown = Math.floor(Math.random() * 11);
    }

}
